{
  "name": "virtual-drumpad",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test",
    "check": "node --check JS/bateria.js && node --check JS/drum-core.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
